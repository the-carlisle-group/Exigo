 AddBaseURL←{
     ⍝ ⍺ ←→ BaseURL
     ⍝ ⍵ ←→ Document(s)
     ⍺←⎕THIS.BaseURL
     ⍺≡'':0
     e←∊A.Elements¨⍵
     _←⍺{
         0=≢⍵:0
         r←⍵.href
         ⍵.href←(''⍺['/'=⊃¨r]),¨r
         0}e/⍨e.Tag∊,¨'a' 'link'
     _←⍺{
         0=≢⍵:0
         r←⍵.src
         ⍵.src←(''⍺['/'=⊃¨r]),¨r
         0}e/⍨e.Tag∊⊂'img'
     0
 }
