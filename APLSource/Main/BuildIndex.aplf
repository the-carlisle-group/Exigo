 BuildIndex←{
     ⍝ ⍺ ←→ Target File, Header
     ⍝ ⍵ ←→ Text, URL
     d←#.Abacus.DocSource.BuildTemplate''
     m←A.GetMain d
     _←m A.New'h1'(1↓∊' ',¨{(⍵∊⎕A)⊂⍵}1⊃⍺)
     n←m A.New'nav'
     l←n A.NewLink¨↓⍉↑⍵
     d PutFile 0⊃⍺
 }
