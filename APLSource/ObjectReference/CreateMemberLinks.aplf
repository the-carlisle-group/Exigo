 CreateMemberLinks←{
     ⍝ ⍺ ←→ Member Type (Property or method)
     ⍝ ⍵ ←→ Class, member list
     c m←⍵
     0=≢m:⍬
     r←('/ObjectReference/',c,'/',⍺,'/')∘,¨m
     l←A.NewLink¨m{⍺ ⍵}¨r
     p←A.New'nav'l
     p.class←'member-list'
     h←A.New'h3'⍺
     h,p
 }
