 GetInfo←{
     ⍝ ⍵ ←→ Info Space
     m←' '~¨⍨↑{⍵⊆⍨⍵≠'_'}¨⍵.Name
     ⍵.(Object MemberType MemberName)←↓⍉m
     i←'OMP'⍳⊃¨⍵.MemberType
     c←i=0
     m[;1]←'' 'Methods' 'Properties'[i]
     p←⍵.ParentSegment∘,¨(-3*c)↓¨,/m,¨'/'
     ⍵.URL←p
     ⍵.TargetFile←p,¨c⌿¨⊂'/index'
     ⍵
 }
